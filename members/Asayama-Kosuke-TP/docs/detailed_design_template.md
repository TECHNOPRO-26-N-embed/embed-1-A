# 詳細設計書 — 組込み開発実習

<!-- 作成者: あなたの名前 / 日付: YYYY-MM-DD / グループ: 〇-〇 -->

> **このドキュメントの目的**
> 基本設計書（basic_design.md）で「**どのような構造で作るか**」を決めました。
> この詳細設計書では「**各処理を具体的にどう実装するか**」を決めます。
> 書き終わったとき、**コードの骨格がほぼ完成している**状態を目指してください。

> [!NOTE]
> **V字モデルにおける位置づけ**
> 詳細設計書 ←→ **単体テスト**（関数・部品ごとのテスト）が対応します。
> 「この関数が正しく動くか」の確認は Section 5 の単体テスト仕様書で計画します。
> ※ 必須機能全体が動くかの「結合テスト」は基本設計書（Section 6）に記載します。

---

## 0. 基本設計書との接続確認

| 項目 | basic_design.md から転記 |
|:--|:--|
| 作品タイトル | 暗証番号ロック装置 |
| 状態の種類（1-2 状態遷移から） | 待機中 / 入力中 / 判定 / 正解 / エラー / ロック |
| 実装する関数の数（2-2 関数一覧から） | 　11個 |
| グローバル変数の合計バイト数（2-1 SRAM確認から） | 　20B |

---

## 1. グローバル変数・定数の設計

> ※ 基本設計書（2-1 データ設計）をもとに、**型と初期値まで**決めます。
> ここで設計した変数は、この後の関数設計でそのまま使います。

```
【ピン定義】（basic_design.md 3-1 から転記）
  PIN_BUTTON    = 2    // タクトスイッチ（INPUT_PULLUP）
  PIN_LED_RED   = 9    // 赤LED
  PIN_LED_GREEN = 10   // 緑LED
  PIN_BUZZER    = 11   // パッシブブザー

【状態管理】（basic_design.md 1-2 の状態名から転記）
  currentState  : int = 0   // 0:待機 1:動作中 2:完了 3:エラー

【タイマー（millis()用）】（basic_design.md 2-3 から転記）
  lastMillis_LED    : unsigned long = 0
  lastMillis_Sensor : unsigned long = 0

【センサー・入力値】（basic_design.md 2-1 から転記）
  sensorValue   : int  = 0
  buttonState   : bool = false

【その他のフラグ・カウンター】
  （自分のものを追加）
  【ピン定義】
  PIN_LED   = 10
  PIN_BUZZER = 11

【状態管理】
  currentState : int = 0
  // 0:待機 1:入力 2:判定 3:成功 4:エラー 5:ロック

【パスワード管理】
  char inputCode[5] = ""
  char correctCode[5] = "1234"
  int inputIndex = 0

【ミス管理】
  int errorCount = 0
  bool isLocked = false

【タイマー】
  unsigned long now = 0
  unsigned long lastMillis = 0

【チャタリング対策】
  unsigned long lastDebounceTime = 0
  const int DEBOUNCE_DELAY = 50
```

---

## 2. 各関数の詳細設計

> ※ 基本設計書（2-2 関数一覧）で定義した各関数の「中身」を設計します。
> **疑似コード**（日本語＋処理の流れ）で書いてください。実際のC++コードは書かなくてOKです。

---

### `setup()` — 初期化処理

```
【処理の流れ】
1. ピンモードを設定する
   - PIN_BUTTON  → INPUT_PULLUP
   - PIN_LED_*   → OUTPUT
   - PIN_BUZZER  → OUTPUT

2. ライブラリの初期化（使うものだけ）
   - 例: lcd.begin(16, 2)
   - 例: servo.attach(PIN_SERVO)

3. Serial.begin(9600)（デバッグ用）

4. 起動確認（任意）: 緑LEDを1秒点灯して消灯
```

**↓ 自分の setup() を設計してください**
```
【処理の流れ】
1. ピンモード設定
   pinMode(PIN_LED, OUTPUT)
   pinMode(PIN_BUZZER, OUTPUT)

2. キーパッド初期化（ライブラリ使用）

3. Serial.begin(9600)

4. 動作確認
   LEDを1秒点灯 → 消灯


```

---

### `loop()` — メインループ

> ※ loop() は「状態ごとに何をするか」だけ書く。細かい処理は各関数に任せる。

```
【処理の流れ】

＜毎ループ実行すること＞
  - 入力を読む（readButton(), readSensor() などを呼ぶ）
  - 現在時刻を取得: now = millis()

＜currentState が 0（待機中）のとき＞
  - センサー値を監視する
  - 検知条件を満たしたら → currentState = 1

＜currentState が 1（動作中）のとき＞
  - メイン処理を行う
  - 終了条件を満たしたら → currentState = 2

＜currentState が 2（完了）のとき＞
  - 完了表示をする
  - リセットボタンが押されたら → currentState = 0

＜currentState が 3（エラー）のとき＞
  - エラー表示をする / リセットを待つ
  【エラー状態に入る条件】

1. ユーザーが4桁の暗証番号を入力する
2. 「#」キーを押して入力を確定する
3. confirmInput() により currentState が「判定」になる
4. checkPassword() が呼ばれる
5. 入力値と正解コードを比較する

6. 比較結果が「不一致」の場合
   → currentState = エラー状態 に遷移する
```

**↓ 自分の loop() を設計してください**
```
【処理の流れ】

＜毎ループ実行すること＞
処理の流れ】

＜毎ループ＞
  now = millis()
  key = readKeypad()

＜STATE_CHECK＞
  パスワード判定
  一致 → STATE_SUCCESS
  不一致 → STATE_ERROR

最終的に updateOutput() を呼ぶ

＜currentState がロック中のとき＞
 lockSystem()
    return

＜currentState が数字入力のとき＞
 inputNumber(key)
    currentState = 入力中


＜currentState が #入力のとき＞
confirmInput()

```

---

### （関数ごとに以下のブロックをコピーして追加してください）

> ※ 基本設計書 2-2 の関数一覧に記載した関数を1つずつ設計します。

---

### `関数名()` — （役割を1行で書く）

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
readKeypad() 
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
char
```
【処理の流れ】
1. キーパッドからキー取得
2. 押されていなければ NO_KEY
3. 押されたキーを返す


【エラー・異常ケース】
- 異常な値が来た場合:
無効キー入力（数字・#以外）
  → NO_KEY を返して無視
- ノイズで誤検知
  → デバウンスで無視

```

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
inputNumber(char key)
**引数：** `引数名`（型）: 何の値か
key（char）：押されたキー
**戻り値：** 型（なしの場合は void）
void
```
処理の流れ】
1. 数字か判定
2. inputIndex < 4 のときのみ追加
3. inputCode[inputIndex] = key
4. inputIndex++

【エラー・異常ケース】
- 異常な値が来た場合:
keyが数字でない
  → 無視
- 入力が4桁以上
  → 無視
- inputIndexが異常値（負または5以上）
  → 0にリセット

```

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
confirmInput() 
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】
1. inputIndex == 0 → 何もしない
2. STATE_CHECK に変更

【エラー・異常ケース】
- 異常な値が来た場合:
- 入力が空
  → 状態変更しない
- 配列が破損している
  → 初期化して待機状態に戻る
```
**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
checkPassword()
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
bool（true:成功 / false:失敗）
```
【処理の流れ】
1. strcmp(inputCode, correctCode)
2. 一致なら true
3. 不一致なら false

【エラー・異常ケース】
- 異常な値が来た場合:
- inputCodeがnullまたは未初期化
  → false（不一致扱い）
- 桁数が4未満
  → false
```
**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
successAction()
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】
1. LED ON
2. 2秒後 LED OFF（millis）
3. errorCount = 0
4. 入力リセット


【エラー・異常ケース】
- 異常な値が来た場合:
- LEDが反応しない
  → 状態そのままで処理継続
- タイマー異常
  → 即時OFF
```
**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
errorAction()
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】
1. ブザー ON
2. 0.5秒後 OFF
3. errorCount++

4. errorCount >= 3
   → STATE_LOCK

【エラー・異常ケース】
- 異常な値が来た場合:
- errorCountが異常（負値など）
  → 0にリセット
- ブザー故障
  → 状態のみ更新
```

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
beepError(int count)
**引数：** `引数名`（型）: 何の値か
count（int）：回数
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】
指定回数だけ
  ブザーON → OFFを繰り返す
【エラー・異常ケース】
- 異常な値が来た場合:
- count <= 0
  → 何もしない
- 過剰な回数（例：100回以上）
  → 上限を設定して制限
```
**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
lockSystem() 
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】
1. 初回時刻を記録
2. now - lastMillis < 10000 → 入力無効
3. 10秒経過 → ロック解除
4. errorCount = 0

【エラー・異常ケース】
- 異常な値が来た場合:
- タイマー値オーバーフロー
  → 強制的にロック解除
- ロック中に入力が来る
  → 完全に無視
```

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）
updateOutput()
**引数：** `引数名`（型）: 何の値か
なし
**戻り値：** 型（なしの場合は void）
void
```
【処理の流れ】

STATE_SUCCESS:
  successAction()

STATE_ERROR:
  errorAction()

STATE_IDLE:
  何もしない
【エラー・異常ケース】
- 異常な値が来た場合:
- 不正な状態値（0〜5以外）
  → 待機状態にリセット
```

---

## 3. 重要ロジックの詳細設計

### 3-1. チャタリング防止（デバウンス処理）

> ※ ボタンを使う場合は必ず設計してください。

```
【処理】
1. 入力取得
2. now - lastDebounceTime < 50ms → 無視
3. OKなら処理
4. lastDebounceTime更新
```

---

### 3-2. millis() を使ったタイマー管理

```
【使用箇所】

LED制御：
  2秒後消灯

ブザー：
  0.5秒

ロック：
  10秒
```

---

## 4. デバッグ出力計画（任意）

> **【任意】** 関数設計（Section 2）と並行して記入すると効果的です。
> 「動かない」ときに何を確認すればいいかを事前に計画しておきます。
> 実装後は不要な Serial.println() を削除すること。

| No | 確認したい内容 | 挿入する関数 | Serial.println の内容例 |
|:---|:---|:---|:---|
| 1 | キー確認 | `readKeypad()` | `key;` |
| 2 | 状態確認 | `loop()` | `currentState` |
| 3 | 入力確認 | `inputNumber()` | `inputCode` |
| 4 | 判定確認 | `checkPassword` | `OK/NG` |

---

## 5. 単体テスト仕様書（V字モデル：詳細設計 ↔ 単体テスト）

> ※ 各関数・部品が「単体で正しく動くか」を確認するテスト項目を設計します。
> 「実際の結果」欄は実装後に記入します。

### 5-1. 入力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | readKeypad() | キー押す | 値取得 | | [ ] |
| 2 | inputNumber() | 4回入力 | 保存 | | [ ] |
| 3 | confirmInput() | #押下 | 判定へ | | [ ] |


### 5-2. 出力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | successAction | 正解 | LED点灯 | | [ ] |
| 2 | errorAction | ミス | ブザー | | [ ] |
| 3 | lockSystem | 3回ミス | 入力無効 | | [ ] |

### 5-3. タイミング・並行動作テスト

| No | テスト内容 | テスト手順 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | LED | LED点滅中にボタンを押す | 2秒点灯 | | [ ] |
| 2 | ブザー | 点滅をストップウォッチで確認 | 0.5秒 | | [ ] |
| 3 | ロック | 点滅をストップウォッチで確認 | 10秒 | | [ ] |

---

## 6. AIレビュー記録

> グループレビューの前に必ず実施してください。

### Q1: 実装上の問題確認

> 「この詳細設計書に書いた関数と処理フローをもとに Arduino でコードを書きます。バグになりやすい箇所・処理の抜け・型の問題はありますか？」

**AIの回答（要約）：**
ロック解除条件と入力リセット忘れに注意
**対応した内容：**
リセット処理を設計に追加
---

### Q2: 単体テスト仕様の確認

> 「Section 5 の単体テスト仕様書で、各関数の動作が正しく検証できていますか？テストが不足している項目や、境界値テストが必要な箇所を教えてください。」

**AIの回答（要約）：**
配列サイズオーバー注意
inputIndex管理が重要
state遷移は問題なし

**対応した内容：**
境界テスト（3→4桁）が不足
lockSystemのテスト追加推奨
---

## 7. グループレビュー記録

### 7-1. 指摘一覧

| No | 指摘内容 | 指摘者 | 対応 |
|:---|:---|:---|:---|
| 1 | AIの回答の書き忘れ | 藤原 | 追記した |
| 2 | 戻り値などが書かれていない | 池田 | 追記した |
| 3 | 異常な値が来た場合:も一つ一つ書かれていない | 干場 | 追記した |
| 4 | エラー表示をする事態は書かれているが、どうやってそのエラーに木塚が書かれていない | 関塚 | 追記した |

### 7-2. レビューを受けて変更した点

-
-

---

*初版: YYYY-MM-DD / AIレビュー: YYYY-MM-DD / グループレビュー後更新: YYYY-MM-DD*
